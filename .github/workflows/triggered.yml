name: Trigger
on:
  workflow_call:
jobs:
  trigger-echo:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: env
      - name: List Files
        run: ls -lah
